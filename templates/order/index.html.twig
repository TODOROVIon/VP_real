{% extends 'base.html.twig' %}


{% block body %}

    <div class="container my-5 small-container">
        <h1>Ma commande</h1>
        
        {# Choix de mon adresse de mon livraison<br/> #}
        {{ form_start(deliveryForm) }}
            {{form_row(deliveryForm.carriers)}}         {# permet de enverser les parties du notre formulaire plus facilement #}
            
            <a href="{{ path('app_account_address_form')}}" class="small">Ajouter une nouvelle addresse de livraison</a>
            <hr>
            
            {{form_row(deliveryForm.addresses)}}        {# permet de enverser les parties du notre formulaire plus facilement #}
        {{ form_end(deliveryForm) }}

        {# Choix du transporteur #}

    </div>    

{% endblock %}
